# PlainView

Windows용 가볍고 광고 없는 이미지 뷰어입니다.

[English README](README.md)

> 이미지를 열면 이미지 자체만 깔끔하게 표시합니다.
> 화면 가장자리로 포인터를 가져가면 필요한 컨트롤만 나타납니다.

## 언어 지원

PlainView는 한국어와 영어 UI를 지원합니다. 시스템/브라우저 언어가 한국어이면 한국어를 사용하고, 그 외 환경에서는 영어를 사용합니다. 개발 중에는 로컬 개발 URL에 `?lang=ko` 또는 `?lang=en`을 붙이거나 `localStorage.plainview.locale` 값을 `ko` 또는 `en`으로 설정해 언어를 강제로 확인할 수 있습니다.

## 지원 형식

JPG, JPEG, PNG, WebP, GIF, BMP, TIF, TIFF, ICO, AVIF, JXL, PSD, TGA, PBM, PGM, PNM, PPM, PAM, DDS

JPG, JPEG, PNG, WebP, GIF, BMP, AVIF는 큰 base64 문자열로 복사하지 않고 WebView asset protocol로 직접 표시합니다.
설치된 WebView 런타임이 AVIF를 표시하지 못하면 PlainView는 AVIF 전용 오류를 명확히 표시합니다.

PAM 지원은 표준 tuple type 중심입니다.
DDS 지원은 DXT1/BC1, DXT3/BC2, DXT5/BC3 계열 중심입니다.
BC4, BC5, BC6H, BC7, 기타 DXGI 변형, 일반 비압축 DDS 파일은 지원하지 않습니다.

HEIC, HEIF, RAW, CR2, NEF, ARW 확장자는 인식하지만, 현재 버전에서는 열 때 지원하지 않는 형식이라는 오류를 명확히 표시합니다.

## 키보드 단축키

| 키 | 동작 |
|---|---|
| `Esc` | 닫기 |
| `Ctrl+O` | 이미지 열기 |
| `Left Arrow` / `Backspace` | 이전 이미지 |
| `Right Arrow` / `Space` | 다음 이미지 |
| `Home` / `End` | 첫 이미지 / 마지막 이미지 |
| `+` | 확대 |
| `-` | 축소 |
| `0` | 원본 크기 |
| `F` | 화면 맞춤 |
| `T` | 항상 위 고정 전환 |
| `R` | 시계 방향 90도 회전 |
| `Ctrl+C` | 이미지·파일 통합 복사 |
| `Ctrl+S` | 다른 이름으로 저장 |
| `Ctrl+M` | 다른 폴더로 이동 |
| `Ctrl+P` | 인쇄 |
| `F2` | 이름 바꾸기 |
| `Alt+Enter` | 파일 속성 |
| `Delete` | 휴지통으로 이동 |
| `F5` | 현재 폴더와 이미지 새로고침 |

## 기능

- **테두리 없는 창** - 이미지를 주 화면으로 유지하는 최소 UI
- **빠른 이미지 열기** - 빈 화면 버튼, 더블클릭, `Ctrl+O`, 드래그 앤 드롭으로 이미지 열기
- **컨텍스추얼 HUD** - 유휴 상태에서는 이미지만 표시하고, 접근한 가장자리의 탐색·창·보기 컨트롤만 노출
- **동적 조작 피드백** - 이미지 이동과 배율 변경 결과만 잠깐 표시하고 설정한 시간 뒤 자동으로 숨김
- **자연스러운 폴더 탐색** - 같은 폴더의 이미지를 `1, 2, 10` 순서로 정렬해 이전/다음 탐색 지원
- **외부 변경 실시간 반영** - 현재 폴더를 감시해 추가·삭제·덮어쓴 파일을 자동 반영하고, 포커스 복귀와 `F5`를 보조 새로고침으로 사용
- **대용량 폴더 대응** - 폴더 스캔과 정렬을 백그라운드에서 처리하고 오래된 이미지 선로딩을 중단
- **확대/축소 컨트롤** - 마우스 휠, 키보드, 버튼으로 확대/축소
- **이미지 패닝** - 확대 상태에서 드래그로 이미지 이동
- **전체 화면** - 더블 클릭으로 전체 화면 맞춤, 다시 더블 클릭하면 이전 배율 복원
- **창 이동** - 기본 모드에서 이미지 영역 드래그, 확대 상태에서는 상단 핸들 또는 Alt-드래그 사용
- **컨텍스트 메뉴** - 이미지·파일 통합 복사와 경로 복사, 탐색기에서 보기, 기본 앱·연결 프로그램·등록 앱으로 열기, 저장·이동·이름 변경·속성 확인·휴지통 이동, PlainView 자체 인쇄 창으로 인쇄, 앱 버전·라이선스 정보 확인; 이미지가 없는 시작 화면에서도 이미지 열기와 앱 정보 사용 가능; 방향키·Tab·Esc 키보드 탐색 지원
- **통합 복사** - `Ctrl+C` 또는 컨텍스트 메뉴 한 번으로 이미지 픽셀과 파일 객체를 함께 복사해 붙여넣는 프로그램에 맞는 형식을 자동 제공
- **파일 경로 복사** - 컨텍스트 메뉴에서 현재 이미지의 전체 경로를 클립보드에 복사
- **다른 이름으로 저장** - `Ctrl+S` 또는 컨텍스트 메뉴로 원본 이미지 파일 저장
- **이름 바꾸기** - 컨텍스트 메뉴에서 이미지 확장자를 유지한 채 파일 이름 변경
- **빠른 파일 이동** - 컨텍스트 메뉴 또는 `Ctrl+M`으로 현재 이미지를 다른 폴더로 이동
- **휴지통 지원** - 컨텍스트 메뉴 또는 `Delete`로 현재 이미지를 휴지통으로 이동
- **GIF 일시정지** - GIF를 클릭해 현재 프레임에서 일시정지하고, 다시 클릭해 재생
- **이미지 정보** - 하단 정보 바에 마우스를 올려 경로, 크기, 파일 크기, 확장자 확인
- **항상 위 고정** - 이미지를 다른 창 위에 유지
- **회전 및 직접 배율 입력** - 보기 전용 90도 회전과 하단 배율 라벨을 통한 숫자 배율 입력
- **드래그 앤 드롭** - 창에 이미지를 끌어 놓아 열기
- **복구 가능한 오류 화면** - 다시 시도, 다음 이미지, 폴더에서 보기로 탐색을 계속 진행
- **간결한 설정 창** - 언어, 기본 보기, 반복 탐색, 창 위치 기억, 조작 피드백 표시 시간을 설정
- **설정 저장** - 창 위치, 항상 위 고정 상태와 보기 설정을 자동 저장
- **기본 앱 설정 도우미** - 설정에서 Windows 기본 앱 화면을 바로 열고 파일 형식별 연결 방법 안내
- **업데이트 확인** - 현재 버전과 GitHub의 최신 공개 버전을 비교하고 새 버전 다운로드 페이지 열기

## 기술 스택

- [Tauri v2](https://tauri.app/) - 가벼운 데스크톱 앱 프레임워크
- [React 19](https://react.dev/) + TypeScript
- [Vite](https://vite.dev/) - 프론트엔드 빌드 도구

## 개발 환경

```text
Rust 1.94+, Node.js 20+, npm 10+
```

## 로컬 실행

```bash
# 의존성 설치
npm install

# 개발 모드 실행
npm run tauri dev

# 프로덕션 빌드
npm run tauri build

# Windows 릴리즈 산출물 생성: setup, MSI, portable EXE
npm run release:windows
```

## 현재 제한 사항

- Windows 정책상 기본 앱 선택은 사용자가 직접 확정해야 하며, PlainView 설정에서 해당 Windows 화면을 바로 열 수 있습니다.
- 업데이트 확인은 사용자가 버튼을 누를 때만 GitHub에 연결하며, 설치는 다운로드 페이지에서 직접 진행합니다.
- PlainView는 보기 전용이며 이미지 편집 도구를 포함하지 않습니다.
- GIF 지원은 클릭으로 일시정지/재생하는 기능으로 제한됩니다. 프레임 단위 탐색은 지원하지 않습니다.
- WebView가 직접 표시하지 못하는 형식은 PNG로 변환한 뒤 base64 데이터로 로드하며, 변환 결과는 5개 이미지 LRU 캐시를 사용합니다.

## 코드 서명 정책

[SignPath.io](https://signpath.io/)가 무료 코드 서명을 제공하고 [SignPath Foundation](https://signpath.org/) 인증서를 사용합니다.

검증된 릴리스 절차는 [코드 서명 정책](CODE_SIGNING_POLICY.md), 실행 중 네트워크 및 데이터 처리 방식은 [개인정보 처리방침](PRIVACY.md)에서 확인할 수 있습니다. 릴리스 설명에 서명된 빌드라고 명시된 파일만 서명된 릴리스이며 이전 릴리스는 서명되지 않았을 수 있습니다.

## 라이선스

MIT
